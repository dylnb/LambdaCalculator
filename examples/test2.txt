Homework 2

variables of type e : x
variables of type <d,et> : D
variables of type <e,t> : P
variables of type d : d
constants of type d : pos

define high : Ld.Lx.high(x,d)
define est1: LD.Lx. GT(pos, max(Ld.D(d)(x)))
define est2: LD.Lx. GT(max(Ld.D(d)(x)), pos)

use rule function application
use rule non-branching nodes
use rule predicate modification
use rule lambda abstraction

exercise tree
title Full Derivations
directions Derive the semantic value of each nonterminal node of the tree.
points per exercise 20

[.S1 high est1 ]
[.S2 high est2 ]